[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "logistigate"
authors = [{name = "Eugene", email = "eugenewickett@gmail.com"}]
dynamic = ["version", "description"]
